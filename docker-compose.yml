services:
  # --- Backends ---
  api-python:
    build: ./api/python-fastapi
    profiles: ["py"]
    ports: ["8000:8000"]

  # --- Frontends ---
  web-react:
    build: ./web/vue-app
    profiles: ["vue"]
    ports: ["3000:3000"]
    environment:
      - VITE_API_URL=http://localhost:8000
